const menu=document.querySelector('.menu');const nav=document.querySelector('#nav');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));document.querySelector('#year').textContent=new Date().getFullYear();const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduced){document.querySelectorAll('.reveal').forEach(el=>el.classList.add('visible'))}else{const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el))}
const banner=document.querySelector('#cookieBanner'),accept=document.querySelector('#cookieAccept');if(banner&&!localStorage.getItem('cdt-cookie-notice'))banner.hidden=false;accept?.addEventListener('click',()=>{localStorage.setItem('cdt-cookie-notice','1');banner.hidden=true});const form=document.querySelector('#contactForm'),status=document.querySelector('#formStatus');form?.addEventListener('submit',event=>{event.preventDefault();status.textContent='';const data=new FormData(form);if(data.get('website')){status.textContent='Thanks.';return}const name=String(data.get('name')||'').trim(),email=String(data.get('email')||'').trim(),business=String(data.get('business')||'').trim(),message=String(data.get('message')||'').trim();if(!name||!email||!message||!/^\S+@\S+\.\S+$/.test(email)){status.textContent='Please add your name, a valid email, and a short message.';return}const body=`Name: ${name}\nBusiness: ${business||'Not provided'}\nEmail: ${email}\n\nWhat they need:\n${message}`;status.textContent='Opening your email app…';window.location.href=`mailto:cruz@cruzdoestech.com?subject=${encodeURIComponent('Website inquiry from '+name)}&body=${encodeURIComponent(body)}`});
